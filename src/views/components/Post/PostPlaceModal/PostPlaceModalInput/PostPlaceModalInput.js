import React from "react";
import styles from "./PostPlaceModalInput.scss";
import classNames from "classnames/bind";

const cx = classNames.bind(styles);

const PostPlaceModalInput = () => {
  return <div></div>;
};

export default PostPlaceModalInput;
